# uv workspace root (ADR-0001). Python packages live under apps/ and sdks/.
[tool.uv]
required-version = ">=0.12.5"
# Supply chain: resolve only versions published at least 7 days ago (matches the Dependabot cooldown).
exclude-newer = "7 days"

[tool.uv.workspace]
members = ["apps/ai-service"]
